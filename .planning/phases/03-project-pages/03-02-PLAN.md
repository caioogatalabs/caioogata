---
phase: 03-project-pages
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/sections/v2/project/ProjectHero.tsx
  - src/components/sections/v2/project/ProjectChallenge.tsx
  - src/components/sections/v2/project/ProjectImpact.tsx
  - src/components/sections/v2/project/ProjectInfoBlock.tsx
  - src/components/sections/v2/project/ProjectCredits.tsx
  - src/components/sections/v2/project/ProjectPageShell.tsx
autonomous: true
requirements: [PROJ-01, PROJ-02]

must_haves:
  truths:
    - "Project hero displays title in 3.5rem Fabio XM, data-stamp, role, technologies, and hero image(s)"
    - "Challenge/solution block shows two distinct text sections with headings"
    - "Impact numbers display as large stat values with labels"
    - "Info block at bottom shows project metadata in a dark bar grid layout"
    - "Credits list team members with LinkedIn links"
  artifacts:
    - path: "src/components/sections/v2/project/ProjectHero.tsx"
      provides: "Hero block with title, data-stamp, metadata, images"
      exports: ["ProjectHero"]
    - path: "src/components/sections/v2/project/ProjectChallenge.tsx"
      provides: "Challenge/solution text block"
      exports: ["ProjectChallenge"]
    - path: "src/components/sections/v2/project/ProjectImpact.tsx"
      provides: "Impact stats display"
      exports: ["ProjectImpact"]
    - path: "src/components/sections/v2/project/ProjectInfoBlock.tsx"
      provides: "Bottom info grid with project metadata"
      exports: ["ProjectInfoBlock"]
    - path: "src/components/sections/v2/project/ProjectCredits.tsx"
      provides: "Team credits with LinkedIn links"
      exports: ["ProjectCredits"]
  key_links:
    - from: "src/components/sections/v2/project/ProjectPageShell.tsx"
      to: "ProjectHero, ProjectChallenge, ProjectImpact"
      via: "import and render in switch statement"
      pattern: "case 'hero'"
    - from: "src/components/sections/v2/project/ProjectInfoBlock.tsx"
      to: "src/content/types.ts"
      via: "uses ProjectItem for metadata display"
      pattern: "project\\.role"
---

<objective>
Build the structural text components for project pages: hero, challenge/solution, impact stats, info block, and credits.

Purpose: These components provide the narrative structure of each project page -- the text-heavy blocks that tell the project story and display metadata.
Output: 5 block components wired into ProjectPageShell, replacing placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-pages/03-CONTEXT.md
@.planning/phases/03-project-pages/03-UI-SPEC.md
@.planning/phases/03-project-pages/03-01-SUMMARY.md

@src/content/types.ts
@src/components/layout/Grid.tsx
@src/components/sections/v2/ProjectCard.tsx
@src/hooks/useInView.ts
</context>

<interfaces>
<!-- Types from Plan 01 that this plan consumes -->

From src/content/types.ts (after Plan 01):
```typescript
export interface ProjectSection {
  type: 'hero' | 'challenge' | 'impact' | 'gallery-staggered' | 'gallery-feature-list' | 'gallery-full-detail'
  heading?: string
  body?: string
  challenge?: string
  solution?: string
  stats?: Array<{ value: string; label: string }>
  rows?: GalleryRow[]
  features?: Array<{ name: string; image: ProjectImage; description: string }>
  image?: ProjectImage
  description?: string
  layout?: '4-8' | '8-4'
}

export interface ProjectItem {
  title: string; slug: string; description: string; role?: string;
  technologies?: string; impact?: string; credits?: ProjectCredit[];
  links?: ProjectLink[]; images: ProjectImage[];
  sections?: ProjectSection[]; year?: string; disabled?: boolean;
}
```

From src/components/layout/Grid.tsx:
```typescript
export const Grid // props: as, className, children
export const GridItem // props: as, span (desktop/12), tabletSpan (/8), mobileSpan (/4), className, style, children
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ProjectHero, ProjectChallenge, and ProjectImpact components</name>
  <files>
    src/components/sections/v2/project/ProjectHero.tsx,
    src/components/sections/v2/project/ProjectChallenge.tsx,
    src/components/sections/v2/project/ProjectImpact.tsx
  </files>
  <read_first>
    - src/content/types.ts (ProjectSection, ProjectItem types — verify they exist from Plan 01)
    - src/components/layout/Grid.tsx (Grid/GridItem API: span, tabletSpan, mobileSpan)
    - src/components/sections/v2/ProjectCard.tsx (data-stamp pattern: PRJ_{year} // {index})
    - src/hooks/useInView.ts (entrance animation trigger pattern)
    - .planning/phases/03-project-pages/03-UI-SPEC.md (Hero, typography, spacing specs)
    - .planning/phases/03-project-pages/03-CONTEXT.md (D-02 hero layout, D-03 metadata, D-04 challenge/solution)
  </read_first>
  <action>
**ProjectHero.tsx** (`'use client'`):

Props: `{ project: ProjectItem; section: ProjectSection; index: number }` where `index` is the project's position among enabled projects (for data-stamp NNN).

Per D-02 and D-03, the hero contains:
- **Data-stamp**: `PRJ_{project.year} // {String(index + 1).padStart(3, '0')}` in Cascadia Mono 12px uppercase `tracking-[0.88px] text-text-tertiary`. Per UI-SPEC: matches ProjectCard data-stamp pattern exactly.
- **Title**: `project.title` in `text-[3.5rem] font-bold leading-[1.15]` with `style={{ fontFamily: 'var(--font-sans)' }}`. Per UI-SPEC Typography: Display role.
- **Role**: `project.role` in `text-base text-text-secondary` (Body role, Fabio XM 16px).
- **Technologies**: `project.technologies` in `font-mono text-xs text-text-tertiary uppercase tracking-wider` (Label role).
- **Hero body**: `section.body` if present, in `text-base text-text-secondary leading-relaxed`.
- **Hero image(s)**: First image from `project.images` (eager loading). If 2+ images in project.images, show first two in 6-6 layout. If 1 image, show full 12-col. Images use `<img>` tag (not next/image for gallery), `loading="eager"`, `rounded-[var(--radius-component-md,12px)]`, `overflow-hidden` on container.

Layout using Grid/GridItem:
- Data-stamp + title + role + tech in a column (12-col span)
- Image(s) below in 12-col (single) or 6-6 (two images)
- Section padding: `pt-20 pb-16` (80px top per UI-SPEC, 64px bottom)

Entrance animations: Title gets `-entrance -slide-up -a-0`, metadata gets `-entrance -fade -a-1`, image gets `-entrance -scale-in -a-2`. Use `useInView` on the section wrapper to add `-inview`.

**ProjectChallenge.tsx** (`'use client'`):

Props: `{ section: ProjectSection }`.

Per D-04 and UI-SPEC:
- Grid layout: `4-8` (heading in 4-col, body in 8-col) on desktop. `tabletSpan={8}` full width, `mobileSpan={4}` full width.
- **Challenge heading**: `"Challenge"` in `text-[1.875rem] font-bold leading-[1.2]` with `style={{ fontFamily: 'var(--font-sans)' }}` (Heading role). Render only if `section.challenge` exists.
- **Challenge body**: `section.challenge` in `text-base text-text-secondary leading-relaxed`.
- **Solution heading**: `"Solution"` same heading style. Render only if `section.solution` exists.
- **Solution body**: `section.solution` in same body style.
- Section spacing: `py-16` (64px vertical).

Use `useInView` for entrance: `-entrance -slide-up`.

**ProjectImpact.tsx** (`'use client'`):

Props: `{ section: ProjectSection }`.

Per UI-SPEC Typography:
- Stats displayed in equal-span columns. If 2 stats: `6-6`. If 3 stats: `4-4-4`. If 4 stats: `3-3-3-3`. Use Grid/GridItem.
- **Stat value**: `text-[3.5rem] font-bold leading-[1.15] text-text-brand` with `style={{ fontFamily: 'var(--font-sans)' }}` (Impact number = Display slot, accent color per UI-SPEC).
- **Stat label**: `font-mono text-base text-text-secondary` (Impact label = Body size at mono font).
- **Section heading**: `"Impact"` in Heading role if desired, or omit heading and let the numbers speak.
- Section spacing: `py-16`.
- Entrance: each stat column gets staggered delay `-a-{i}`.

Only render if `section.stats` exists and has items.
  </action>
  <verify>
    <automated>cd /Users/caioogata/Projects/portolio-v1 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ProjectHero.tsx exists and exports `ProjectHero`
    - ProjectHero.tsx contains `PRJ_` for data-stamp pattern
    - ProjectHero.tsx contains `text-[3.5rem]` for Display typography
    - ProjectHero.tsx contains `fontFamily: 'var(--font-sans)'`
    - ProjectHero.tsx contains `loading="eager"` on hero image
    - ProjectHero.tsx contains `rounded-[var(--radius-component-md,12px)]`
    - ProjectHero.tsx uses `useInView` hook
    - ProjectChallenge.tsx exists and exports `ProjectChallenge`
    - ProjectChallenge.tsx contains `Challenge` and `Solution` heading text
    - ProjectChallenge.tsx uses Grid/GridItem with span={4} and span={8}
    - ProjectImpact.tsx exists and exports `ProjectImpact`
    - ProjectImpact.tsx contains `text-text-brand` for stat values
    - ProjectImpact.tsx contains `font-mono` for stat labels
    - All three files contain `'use client'`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Hero shows title with data-stamp + role + tech + image(s). Challenge shows structured challenge/solution text. Impact shows big stat numbers with labels.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProjectInfoBlock, ProjectCredits, and wire all components into Shell</name>
  <files>
    src/components/sections/v2/project/ProjectInfoBlock.tsx,
    src/components/sections/v2/project/ProjectCredits.tsx,
    src/components/sections/v2/project/ProjectPageShell.tsx
  </files>
  <read_first>
    - src/content/types.ts (ProjectItem, ProjectCredit, ProjectLink types)
    - src/content/en.json (verify credits and links data structure per project)
    - src/components/sections/v2/project/ProjectPageShell.tsx (current placeholder version from Plan 01)
    - src/components/sections/v2/project/ProjectHero.tsx (from Task 1 — verify export name)
    - src/components/sections/v2/project/ProjectChallenge.tsx (from Task 1)
    - src/components/sections/v2/project/ProjectImpact.tsx (from Task 1)
    - .planning/phases/03-project-pages/03-UI-SPEC.md (Info Block and Credits specs)
    - .planning/phases/03-project-pages/03-CONTEXT.md (D-12 info block, D-13 credits)
  </read_first>
  <action>
**ProjectInfoBlock.tsx** (`'use client'`):

Props: `{ project: ProjectItem }`.

Per D-12 and UI-SPEC:
- Background: `bg-bg-surface-secondary` — tonal stacking, no border.
- Padding: `py-8 px-5 md:px-8 lg:px-16` (32px vertical per UI-SPEC).
- Section heading: `"Project Details"` in Heading role (`text-[1.875rem] font-bold`, Fabio XM).
- Grid layout for metadata columns. Desktop: 5 columns within 12-col grid using GridItem spans `3-2-3-2-2` for Project, Client & Role, Technologies, Year, Links.
- **Column headers**: `font-mono text-xs uppercase tracking-wider text-text-tertiary` (Label role).
- **Column values**: `text-base text-text-primary` with `style={{ fontFamily: 'var(--font-sans)' }}` (Body role, Fabio XM).
- **Project column** (span 3): `project.title`
- **Client & Role column** (span 2): `project.role || '—'`
- **Technologies column** (span 3): `project.technologies || '—'`
- **Year column** (span 2): `project.year || '—'`
- **Links column** (span 2): map over `project.links`, each as `<a href={link.url} target="_blank" rel="noopener noreferrer" className="block text-text-brand hover:underline transition-colors">{link.label}</a>`
- Mobile responsive: `mobileSpan={4}` on all columns — stacks into full-width label+value pairs. On mobile, render each column as a pair: header text above value text, separated by `mb-4` spacing.
- Tablet: `tabletSpan={4}` — creates 2-column grid of pairs.

**ProjectCredits.tsx** (`'use client'`):

Props: `{ project: ProjectItem }`.

Per D-13 and UI-SPEC:
- Only render if `project.credits` exists and has items.
- Section heading: `"Credits"` in Heading role.
- Spacing: `pt-12 pb-16 px-5 md:px-8 lg:px-16` (48px above per UI-SPEC).
- Each credit: `{credit.name}` followed by `{credit.role}` if present, separated by ` — `.
- If credit has `url`: wrap name in `<a href={credit.url} target="_blank" rel="noopener noreferrer" className="text-text-secondary hover:text-text-primary hover:underline transition-colors">{credit.name}</a>`.
- If no url: plain `<span className="text-text-secondary">{credit.name}</span>`.
- Spacing between credits: `space-y-2` (8px per UI-SPEC).

**Update ProjectPageShell.tsx**:

Replace ALL placeholder blocks with real component imports. The shell must now:

1. Import all 5 components: `ProjectHero`, `ProjectChallenge`, `ProjectImpact`, `ProjectInfoBlock`, `ProjectCredits`.
2. Compute `projectIndex` — the position of this project among enabled projects (for data-stamp):
```typescript
import content from '@/content/en.json'
import type { Content } from '@/content/types'
const typedContent = content as unknown as Content
const enabledProjects = typedContent.projects.items.filter(p => !p.disabled)
```
Then inside the component: `const projectIndex = enabledProjects.findIndex(p => p.slug === project.slug)`

3. Update the `SectionBlock` switch:
   - `case 'hero'`: `<ProjectHero project={project} section={section} index={projectIndex} />`
   - `case 'challenge'`: `<ProjectChallenge section={section} />`
   - `case 'impact'`: `<ProjectImpact section={section} />`
   - `case 'gallery-staggered'`: Keep placeholder (Plan 03)
   - `case 'gallery-feature-list'`: Keep placeholder (Plan 03)
   - `case 'gallery-full-detail'`: Keep placeholder (Plan 03)

4. Replace info block placeholder with `<ProjectInfoBlock project={project} />`
5. Replace credits placeholder with `<ProjectCredits project={project} />`
6. Keep navigation placeholder (Plan 03)
7. Remove the standalone `<h1>` title rendering at top (hero handles it now)
  </action>
  <verify>
    <automated>cd /Users/caioogata/Projects/portolio-v1 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ProjectInfoBlock.tsx exists and exports `ProjectInfoBlock`
    - ProjectInfoBlock.tsx contains `bg-bg-surface-secondary`
    - ProjectInfoBlock.tsx contains `Project Details` heading text
    - ProjectInfoBlock.tsx contains column headers: `Project`, `Client & Role`, `Technologies`, `Year`, `Links`
    - ProjectInfoBlock.tsx uses Grid or GridItem components
    - ProjectInfoBlock.tsx contains `target="_blank"` on link elements
    - ProjectCredits.tsx exists and exports `ProjectCredits`
    - ProjectCredits.tsx contains `Credits` heading text
    - ProjectCredits.tsx contains `credit.url` conditional rendering
    - ProjectCredits.tsx contains `hover:underline`
    - ProjectPageShell.tsx imports ProjectHero, ProjectChallenge, ProjectImpact, ProjectInfoBlock, ProjectCredits
    - ProjectPageShell.tsx no longer contains `data-block="hero"` placeholder div
    - ProjectPageShell.tsx still contains gallery placeholder blocks (data-block="gallery-staggered" etc.)
    - ProjectPageShell.tsx still contains navigation placeholder
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Info block shows project metadata in grid columns. Credits list team members with links. Shell renders Hero, Challenge, Impact, InfoBlock, Credits as real components. Gallery and navigation placeholders remain for Plan 03.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Navigate to `/projects/azion-website` — hero shows title, data-stamp, role, tech, and hero image
3. Challenge/solution section displays structured text with headings
4. Impact section shows stat numbers (if project has stats)
5. Info block at bottom shows metadata grid with project, role, tech, year, links
6. Credits section lists team members with LinkedIn links
7. Gallery placeholders still visible (to be implemented in Plan 03)
</verification>

<success_criteria>
- Hero renders with 3.5rem title, data-stamp, role, technologies, and at least one image
- Challenge block shows challenge + solution text with headings
- Impact block shows stats as large numbers with labels (when stats exist)
- Info block displays 5-column metadata grid on desktop, stacks on mobile
- Credits render team members with clickable LinkedIn links
- All components use semantic tokens (no raw colors)
- Entrance animations fire on scroll via useInView
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-pages/03-02-SUMMARY.md`
</output>
