---
phase: 04-core-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/v2/AboutSection.tsx
  - src/components/layout/PageShell.tsx
autonomous: true
requirements:
  - SECT-01

must_haves:
  truths:
    - "About section renders bio text in right column with generous line-height"
    - "Left column shows heading 'About' and stacked expertise list in uppercase label-sm"
    - "1-2 pull quotes break the bio flow with display-sm Fabio XM Light on tonal shift background"
    - "Section uses dark theme (no data-theme attribute) and has entrance animations"
  artifacts:
    - path: "src/components/sections/v2/AboutSection.tsx"
      provides: "About section component with 4-8 grid, pull quotes, expertise"
      min_lines: 80
    - path: "src/components/layout/PageShell.tsx"
      provides: "Updated shell rendering AboutSection after ProjectsGrid"
      contains: "AboutSection"
  key_links:
    - from: "src/components/layout/PageShell.tsx"
      to: "src/components/sections/v2/AboutSection.tsx"
      via: "import and render"
      pattern: "import.*AboutSection"
    - from: "src/components/sections/v2/AboutSection.tsx"
      to: "src/content/en.json"
      via: "content import"
      pattern: "import content from.*en.json"
---

<objective>
Build the About section (`whoami`) as a V2 section component with 4-8 grid layout, pull quotes, and expertise list. Wire it into PageShell after ProjectsGrid.

Purpose: The About section is the first content section a hiring manager reads after seeing the project showcase. It communicates the candidate's positioning and breadth.
Output: `AboutSection.tsx` component rendering in PageShell
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-content-sections/04-CONTEXT.md
@src/components/sections/v2/IntroSection.tsx
@src/components/layout/PageShell.tsx
@src/components/layout/Grid.tsx
@src/content/types.ts
@src/hooks/useInView.ts
@src/app/globals.css

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/content/types.ts:
```typescript
export interface Content {
  about: {
    command: string
    heading: string
    bio: string
    expertise: string[]
  }
}
```

From src/hooks/useInView.ts:
```typescript
export function useInView(options?: { threshold?: number; rootMargin?: string; once?: boolean }): React.RefObject<HTMLElement>
// Adds `-inview` class to element when visible. Use with `-entrance -slide-up`, `-entrance -fade`, `-entrance -scale-in` CSS classes.
```

From src/components/layout/Grid.tsx:
```typescript
// <Grid> renders a 12-col grid. <GridItem span={N}> for desktop column spans.
// Props: span (desktop), tabletSpan, mobileSpan, className
```

From src/components/layout/PageShell.tsx (current):
```typescript
export function PageShell() {
  useFontReady()
  return (
    <>
      <IntroSection />
      <MenuSection content={typedContent.menu} />
      <ProjectsGrid />
    </>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AboutSection component</name>
  <files>src/components/sections/v2/AboutSection.tsx</files>
  <read_first>
    - src/components/sections/v2/IntroSection.tsx (pattern for 'use client', useInView, entrance animations, font-sans usage)
    - src/content/en.json (about.bio text to select pull quotes from, about.expertise array)
    - src/content/types.ts (Content.about shape)
    - src/components/layout/Grid.tsx (Grid/GridItem usage)
    - src/hooks/useInView.ts (hook signature)
    - src/app/globals.css (entrance animation classes available)
  </read_first>
  <action>
Create `src/components/sections/v2/AboutSection.tsx` as a `'use client'` component:

**Structure (per D-01, D-05, D-19, D-20, D-21, D-22, D-23):**
```
<section id="about" aria-label="About" className="px-5 md:px-8 lg:px-16 py-20 md:py-28 lg:py-36">
  <Grid>
    <GridItem span={4} tabletSpan={4} mobileSpan={4}>  -- left column
    <GridItem span={8} tabletSpan={4} mobileSpan={4}>  -- right column
  </Grid>
</section>
```
No `data-theme` attribute (dark default per D-05).

**Left column (per D-03):**
- Heading "About" in `heading-sm` style: `style={{ fontFamily: 'var(--font-sans)' }}`, `text-lg font-semibold uppercase tracking-[1.2px] text-text-primary mb-8`
- Expertise list: `content.about.expertise.map()` rendering each as `text-xs uppercase tracking-[1px] text-text-secondary font-medium` (`label-sm` equivalent). Vertical stack with `space-y-3`. Each item gets `-entrance -fade` with stagger classes `-a-0` through `-a-6`.

**Right column (per D-02, D-04):**
- Bio text from `content.about.bio`. Split into paragraphs by splitting on `\n\n` or by natural break points. Render as `<p>` tags with class `text-base leading-relaxed text-text-secondary mb-6` and `style={{ fontFamily: 'var(--font-sans)', fontWeight: 300 }}`.
- **Pull quotes (per D-02):** Extract 2 sentences from the bio as pull quotes. Good candidates:
  1. "My work bridges brand strategy, product craft, and technical implementation" (opening sentence, captures positioning)
  2. "I believe the best digital products are built by people who can see the full picture" (if present, or another strong declarative sentence)
- Pull quote rendering: A `<blockquote>` block inserted between bio paragraphs. Style: `bg-bg-surface-secondary rounded-[12px] p-6 md:p-8 my-8` with text in `text-xl md:text-2xl font-light leading-snug text-text-primary` and `style={{ fontFamily: 'var(--font-sans)', fontWeight: 300 }}`. This creates the tonal shift background block per D-02.

**Animations (per D-06):**
- Two separate `useInView` refs: one for left column wrapper, one for right column wrapper.
- Left column heading: `-entrance -slide-up -a-0`
- Expertise items: `-entrance -fade` with `-a-0` through `-a-6` (stagger)
- Right column paragraphs: `-entrance -slide-up -a-1`
- Pull quote: `-entrance -scale-in -a-2`

**Content import:**
```typescript
import content from '@/content/en.json'
```
Access `content.about.bio`, `content.about.expertise`.
  </action>
  <verify>
    <automated>cd /Users/caioogata/Projects/portolio-v1 && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - AboutSection.tsx exists at src/components/sections/v2/AboutSection.tsx
    - File contains `'use client'` directive on first line
    - File contains `id="about"` on the section element
    - File contains `import { useInView } from '@/hooks/useInView'`
    - File contains `import content from '@/content/en.json'`
    - File contains `import { Grid, GridItem } from '@/components/layout/Grid'`
    - File contains `span={4}` and `span={8}` for the 4-8 grid layout
    - File contains `px-5 md:px-8 lg:px-16` padding pattern
    - File contains at least one `<blockquote` element (pull quote)
    - File contains `bg-bg-surface-secondary` (pull quote tonal shift)
    - File contains `-entrance -slide-up` class usage
    - File contains `-entrance -fade` class usage
    - File contains `expertise` mapping (content.about.expertise.map)
    - File contains `fontFamily: 'var(--font-sans)'` for display text
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>AboutSection renders 4-8 grid with heading + expertise list in left column, bio paragraphs with pull quotes in right column, all with entrance animations</done>
</task>

<task type="auto">
  <name>Task 2: Wire AboutSection into PageShell</name>
  <files>src/components/layout/PageShell.tsx</files>
  <read_first>
    - src/components/layout/PageShell.tsx (current render order)
    - src/components/sections/v2/AboutSection.tsx (just created, verify export name)
  </read_first>
  <action>
Update `src/components/layout/PageShell.tsx` (per D-19):

1. Add import: `import { AboutSection } from '@/components/sections/v2/AboutSection'`
2. Render `<AboutSection />` after `<ProjectsGrid />` in the JSX return:

```typescript
return (
  <>
    <IntroSection />
    <MenuSection content={typedContent.menu} />
    <ProjectsGrid />
    <AboutSection />
  </>
)
```

No props needed — AboutSection imports content directly (same pattern as IntroSection).
  </action>
  <verify>
    <automated>cd /Users/caioogata/Projects/portolio-v1 && npx tsc --noEmit --pretty 2>&1 | head -20 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PageShell.tsx contains `import { AboutSection } from '@/components/sections/v2/AboutSection'`
    - PageShell.tsx contains `<AboutSection />` after `<ProjectsGrid />`
    - TypeScript compiles without errors
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>AboutSection appears on the home page between ProjectsGrid and the page end, with dark theme background providing tonal rhythm</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `pnpm build` succeeds
- About section visible at localhost:3000 below projects grid
- 4-8 grid layout renders correctly on desktop
- Pull quote has tonal shift background (bg-surface-secondary)
- Expertise list stacks vertically in left column
</verification>

<success_criteria>
- AboutSection.tsx exists with 4-8 grid, pull quotes, expertise list
- PageShell renders AboutSection after ProjectsGrid
- Project builds without errors
- Section has `id="about"` for future scroll-to functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-content-sections/04-01-SUMMARY.md`
</output>
