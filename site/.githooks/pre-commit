#!/bin/sh
# Regenerate the build stamp shown in the site's welcome bar.
# Git runs hooks from the repo root, so anchor to site/ explicitly.
cd "$(git rev-parse --show-toplevel)/site" || exit 0
node scripts/update-build-info.mjs
git add src/lib/build-info.ts
