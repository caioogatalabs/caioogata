---
phase: 2
slug: home-page
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-03
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for the Home Page. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom components — shadcn explicitly out of scope per REQUIREMENTS.md) |
| Preset | not applicable |
| Component library | none — custom components matching brutalista aesthetic |
| Icon library | none — stroke-based sharp icons, inline SVG only (Design.md: "Icons must be sharp, stroke-based") |
| Font | Fabio XM (variable, 300-900) as `--font-sans`; Cascadia Mono as `--font-mono` accent |

---

## Spacing Scale

Declared values from `src/tokens/primitives.css` (strict 4px grid, already implemented):

| Token | CSS Variable | Value | Usage in Phase 2 |
|-------|-------------|-------|-------------------|
| space-0 | `--primitive-space-0` | 0px | Touching elements (tonal stacking boundaries) |
| space-1 | `--primitive-space-1` | 4px | Icon gaps, inline padding |
| space-2 | `--primitive-space-2` | 8px | Compact element spacing, tag padding |
| space-3 | `--primitive-space-3` | 12px | Menu item internal padding |
| space-4 | `--primitive-space-4` | 16px | Default element spacing, mobile grid margin |
| space-6 | `--primitive-space-6` | 24px | Section internal padding |
| space-8 | `--primitive-space-8` | 32px | Layout gaps, tablet grid margin |
| space-12 | `--primitive-space-12` | 48px | Major section breaks |
| space-16 | `--primitive-space-16` | 64px | Page-level spacing, desktop grid margin |
| space-20 | `--primitive-space-20` | 80px | Intro section vertical padding |
| space-24 | `--primitive-space-24` | 96px | Hero-to-content gap |

Semantic spacing tokens are already mapped in `src/tokens/semantic.css`:
- Component scale: `--spacing-component-xs` (4px) through `--spacing-component-xl` (32px)
- Block scale: `--spacing-block-sm` (32px) through `--spacing-block-xl` (96px)
- Layout scale: `--spacing-layout-xl` (128px)

Exceptions:
- Project card grid gap: 20px (D-19 — use `gap: 20px` directly, not a spacing token)
- Arrow button touch target: 44px minimum (accessibility)

---

## Typography

All sizes reference `--primitive-text-*` tokens. Font: Fabio XM via `--font-sans`.

| Role | Size | Weight | Line Height | Letter Spacing | Usage |
|------|------|--------|-------------|----------------|-------|
| Display | 72px (`--primitive-text-6xl` = 3.5rem, scaled to 4.5rem) | 400 (Regular) | 1.0 | -0.02em | Intro headline "Bridging brand strategy..." (D-05) |
| Heading | 30px (`--primitive-text-3xl`) | 500 (Medium) | 1.15 (`--primitive-leading-tight`) | -0.01em | Menu section labels, section titles |
| Body | 16px (`--primitive-text-base`) | 400 (Regular) | 1.5 (`--primitive-leading-normal`) | 0 | Menu descriptions, footer text, form labels |
| Overline | 14px (`--primitive-text-sm`) | 500 (Medium) | 1.3 (`--primitive-leading-snug`) | 0.08em | Overline text, data-stamps, tech tags — UPPERCASE (D-05, Design.md) |

Responsive type scaling:
- Display: 72px desktop, 48px tablet, 36px mobile (fluid via `clamp()`)
- Heading: 30px desktop, 24px tablet/mobile
- Body and Overline: fixed across breakpoints

Monospace accent (`--font-mono`): used for CLI input field text, keyboard navigation hints, data-stamp format `PRJ_YEAR // NNN`.

---

## Color

Token system is fully implemented in `src/tokens/semantic.css`. Dark mode is default and only mode for V1 launch.

| Role | Semantic Token | Primitive Source | Usage |
|------|---------------|------------------|-------|
| Dominant (60%) | `--color-bg` | `neutral-500` (oklch 0.17) | Page background — the canvas |
| Secondary (30%) | `--color-bg-surface-primary` | `neutral-400` (oklch 0.36) | Project cards (D-21), menu rows, footer background |
| | `--color-bg-surface-secondary` | `neutral-600` (oklch 0.15) | Intro section background (D-01), sticky header |
| Accent (10%) | `--color-bg-fill-primary` | `brand-400` (oklch 0.91, #FAEA4D) | See reserved-for list below |
| Destructive | `--color-bg-fill-danger` | `red-600` | Form validation errors only |

**Accent (brand yellow #FAEA4D) reserved for:**
1. "Ask about" pill CTA button background (D-03)
2. Contact pill button in footer (D-22)
3. Arrow buttons on project cards (D-20) — on hover only
4. Active/focused menu item highlight
5. Text selection highlight (`::selection` background)
6. Form input focus state flash (Design.md: input focus shifts to yellow)
7. CLI input caret / cursor blink

**Accent must NOT be used for:** body text, section backgrounds (except Intro inverted variant which is deferred), borders, or decorative elements.

Text color mapping:
- Primary text: `--color-text-primary` (neutral-100, oklch 0.78)
- Secondary text: `--color-text-secondary` (neutral-200, oklch 0.65) — descriptions, metadata
- Tertiary text: `--color-text-tertiary` (neutral-300, oklch 0.47) — large text only, 18px+ (AA contrast note in semantic.css)
- Brand text: `--color-text-brand` (brand-400) — links, active states
- On-primary text: `--color-text-on-primary` (brand-950) — text on yellow backgrounds
- Inverse text: `--color-text-inverse` (neutral-800) — text on light surfaces

---

## Grid System

Already defined in tokens. 12-column grid system per CLAUDE.md constraint.

| Breakpoint | Columns | Gap | Margin | Max Width |
|-----------|---------|-----|--------|-----------|
| Desktop (>=1024px) | 12 | 20px | 64px | 1200px |
| Tablet (>=768px) | 8 | 20px | 32px | fluid |
| Mobile (<768px) | 4 | 16px | 20px | fluid |

Layout patterns used in Phase 2:
- Intro: full-width (12col bleed)
- Menu: 2-8-2 (menu list in center 8 columns)
- Projects grid: 6-6 per row (D-19)
- Footer: 2-8-2 (content in center 8 columns)
- Sticky header: full-width with internal 2-8-2 alignment

---

## Component Inventory

### 1. IntroSection

| Property | Value |
|----------|-------|
| Background | `--color-bg-surface-secondary` (D-01) |
| Layout | Full-width, full-viewport-height on load |
| Top bar | Logo left, version "V2.0.01" + tagline right (D-02) |
| Headline | Display 72px, Fabio XM Regular (D-05) |
| Overline | 14px Medium uppercase, above headline (D-05) |
| CTA | "Ask about" pill button, `--radius-component-full` (999px), links to `/llms.txt` (D-03) |
| Scroll behavior | Compacts to sticky header bar on scroll (D-04) |

### 2. StickyHeader (Intro compact state)

| Property | Value |
|----------|-------|
| Position | `position: sticky; top: 0` |
| Height | 56px |
| Background | `--color-bg-surface-secondary` with `backdrop-filter: blur(20px)` at 60% opacity (Design.md glassmorphism) |
| Content | Logo, version, tagline, "Ask about" CTA (D-04) |
| Z-index | 50 |
| Transition | Smooth crossfade from full Intro — not a jarring swap |

### 3. MenuSection

| Property | Value |
|----------|-------|
| Layout | Tabular list, rows not buttons |
| Row height | 64px minimum |
| Typography | Heading size (30px) for labels, Body (16px) for descriptions |
| Keyboard | Arrow keys navigate, Enter selects, Esc returns home, type-to-filter (D-08, D-13) |
| CLI input | Monospace font, blinking caret, filter functionality |
| Hover behavior | Non-hovered rows dim to `--color-text-tertiary` over 0.3s (D-18) |
| Floating preview | Fixed-position image follows cursor, 500px wide, ~5:3 aspect ratio (D-06, D-07) |
| Preview skew | Velocity-based, clamped to +-30deg (D-18, fiddle extraction) |
| Mobile | No floating preview — tap navigates directly (D-09) |
| Routes | Each item navigates to its own route: `/about`, `/experience`, etc. (D-10) |

### 4. ProjectCard

| Property | Value |
|----------|-------|
| Dimensions | 6-column span, 420px height (D-19) |
| Background | `--color-bg-surface-primary` (D-21) |
| Border radius | `--radius-component-md` (12px) (D-21) |
| Title | Top-left, Heading weight |
| Data-stamp | Top-right, Overline style, format `PRJ_YEAR // NNN` (D-20) |
| Arrow button | Bottom-right, `--radius-component-full` (999px pill), 44px touch target (D-20) |
| Grid | 2x2 grid, 20px gap between cards (D-19) |

### 5. FooterSection

| Property | Value |
|----------|-------|
| Closed state | Tech tags (static list: Next.js, React, Tailwind, Vercel), copyright, Contact pill button (D-22, D-26) |
| Tech tags | Overline style, pill-shaped with `--radius-component-full` |
| Contact button | Brand yellow background, on-primary text, pill radius |
| Expanded state | Grows upward revealing contact form + social links (D-23) |
| Trigger | Click Contact button OR scroll past footer OR `/contact` from CLI menu (D-23, D-24) |
| Form fields | Name, email, message — underline-only styling (Design.md) |
| Submit button | Brand yellow, "Send message" label |

### 6. NavigationBar (keyboard hints)

| Property | Value |
|----------|-------|
| Position | Fixed bottom or integrated with menu |
| Content | Esc to go back, arrows to navigate, Enter to select (D-13) |
| Typography | Overline style, monospace |
| Persistence | Visible on all routes (D-13) |

---

## Animation Contract

CSS-first system inspired by fiddle.digital (D-14). No external animation runtime for entrance/scroll animations.

### Easing Tokens (add to globals.css)

| Token | Value | Usage |
|-------|-------|-------|
| `--ease-out` | `cubic-bezier(0.22, 0.31, 0, 1)` | Entrance animations, page transitions — 0.9s duration (D-15) |
| `--ease-in` | `cubic-bezier(0.69, 0, 0, 1)` | Exit animations, cursor image scale |
| `--ease-smooth` | `cubic-bezier(0.5, 0, 0.3, 1)` | Micro-interactions — 0.3-0.45s duration (D-15) |

### Entrance System

| Pattern | Initial State | Duration | Easing | Trigger |
|---------|--------------|----------|--------|---------|
| Slide up | `translateY(4rem), opacity: 0` | 0.9s | `--ease-out` | `-inview` class via IntersectionObserver |
| Scale in | `scale(0.5), opacity: 0` | 0.9s | `--ease-out` | `-inview` class |
| Fade | `opacity: 0` | 0.9s | linear | `-inview` class |
| Page enter | `opacity: 0, translateY(32px)` | 0.6s | `--ease-out` | `-ready` class after fonts load (D-17) |

### Stagger System (D-16)

- Base delay: 70ms per element index
- Base offset: 150ms (first element starts at 150ms, not 0ms)
- CSS classes: `-a-0` through `-a-20`
- Formula: `transition-delay: calc(70ms * var(--stagger-index) + 150ms)`

### Loading Gate (D-17)

```
html                          -> Initial
html.-loaded                  -> Assets loaded
html.-loaded.-ready           -> Ready to animate (fonts confirmed)
```

No entrance animation fires until `-ready` is present on `<html>`.

### Specific Interactions (Implemented)

#### Menu Row Hover — Fiddle-Style (reference: fiddle.digital/work)

**Visual effect (4 coordinated animations):**
1. **Background bar** — Brand yellow (`--color-bg-fill-primary`) scales in from `scaleX(0.92) scaleY(0.6)` to `scaleX(1) scaleY(1)`, origin left-center
2. **Text clip reveal** (masked vertical swap) — Primary text slides up (`translateY(-120%)`), duplicate (2xl/1.5rem bold) slides in from below. Applied to BOTH label and description.
3. **Color inversion** — Hovered row text becomes `--color-text-on-primary` (dark on yellow), non-hovered rows dim to `--color-text-tertiary`
4. **Arrow indicator** — Display-size `→` (3.5rem, Fabio XM, weight 400) expands from `width: 0` to `3rem` before the label

**Bar geometry:**
- Covers 4-4 columns: `width: 66.66%`, left offset 12px
- Bleeds vertically: `top: -5px, bottom: -5px` (overflows row bounds)
- Border radius: 6px
- Dividers hide on hovered row (`opacity: 0`)

**Layout:**
- Label: `w-1/3` (aligns description at 4-4-4 grid second block)
- Clip mask height: `2.4rem` (fits the 1.5rem duplicate text)

**Timing:**
- Text reveal entry: `1.0s cubic-bezier(0.16, 1, 0.3, 1)` with 40ms delay — strong deceleration at end
- Text reveal exit: `1.0s cubic-bezier(0.16, 1, 0.3, 1)` with 60ms delay
- Bar entry: `0.6s cubic-bezier(0.22, 0.31, 0, 1)` with 40ms delay
- Bar exit: `0.5s cubic-bezier(0.22, 0.31, 0, 1)` with 60ms delay
- Arrow width: `0.5s` entry with 40ms delay, `0.3s` exit
- Color transitions: `0.3s cubic-bezier(0.22, 0.31, 0, 1)`

**Keyboard parity:**
- `activeIndex` (ArrowUp/Down) triggers identical visual effect when no mouse hover active
- `isHighlighted = hoveredIndex === index || (hoveredIndex === null && activeIndex === index)`
- Esc resets `activeIndex` to null (deactivates highlight)
- Mouse leave deactivates `hoveredIndex` (existing behavior)

#### Floating Preview — Elastic Cursor Follow (reference: fiddle.digital/work)

**Architecture:** `position: fixed; top: 0; left: 0; z-index: 100`, positioned via `transform: translate3d()` in rAF loop. Lives inside MenuSection, desktop-only (`hidden lg:block`).

**Lerp system (creates elastic "bounce"):**
- `requestAnimationFrame` loop interpolates position: `lerp(current, target, 0.12)`
- Target = raw `clientX/clientY` from mousemove
- Lerped position creates natural lag — image trails cursor with elastic feel
- On first show: snaps lerp to cursor position (avoids fly-in from origin)

**Skew (velocity-based distortion):**
- Computed from delta of lerped position (not raw mouse delta — smoother)
- Applied on BOTH axes: `skew(skewX, skewY)`
- Each axis: `clamp(-30°, -delta * 1.2, 30°)`

**Entry animation (zoom reveal):**
- Wrapper: `scale: 0.5` → `scale: 1`, 0.6s `cubic-bezier(0.16, 1, 0.3, 1)` — starts at 50% size, strong deceleration
- Inner image: `scale: 3` → `scale: 1.25`, 0.7s `cubic-bezier(0.16, 1, 0.3, 1)` — zoom-out reveal, slowest/most cinematic
- Opacity: `0` → `1`, 0.2s `cubic-bezier(0.22, 0.31, 0, 1)`
- `transformOrigin: center center` — expands from center of image

**Re-open on row switch:**
- `key={imageSrc}` forces React to unmount/remount `RevealImage` component
- Each new image gets a fresh DOM element starting at `scale: 0.5`
- Guarantees the expand animation replays on every row change

**Exit animation:**
- Container opacity: `1` → `0`, 0.15s `cubic-bezier(0.69, 0, 0, 1)` (fast fade, no scale — key remount handles cleanup)

**Inner image parallax:**
- Resting scale: `1.25` (via CSS `scale` property — separate from rAF `transform`)
- rAF sets `transform: translate3d(parallaxX, parallaxY, 0)` based on inverse velocity
- Parallax range: `clamp(-32px, velocity * -0.4, 32px)` per axis

**Dimensions:** 500×300px, border-radius 0px (brutalista, sharp corners), `overflow: hidden`

**Reduced motion:** Lerp disabled (instant position), skew zeroed, parallax zeroed

#### Other Interactions

| Element | Interaction | Spec |
|---------|------------|------|
| Arrow buttons | Hover | Background transitions to brand yellow, icon color to on-primary, 0.3s `--ease-smooth` |
| Contact expand | Footer grow | max-height transition upward, 0.6s `--ease-out`, triggered by scroll-to-bottom or Contact button |
| Intro compact | Scroll | Smooth transition from full intro to sticky header, opacity crossfade at 60% scroll |
| Page transition | Route change | 0.6s opacity + translateY(32px), gated behind `-ready` (D-17) |

### Reduced Motion

All animations suppressed via existing `prefers-reduced-motion` rule in `globals.css`. Already implemented — instant state changes, no motion.

---

## Copywriting Contract

Content sourced from `src/content/en.json` (D-28 — no content rewrite).

| Element | Copy | Source |
|---------|------|--------|
| Display headline | "Bridging brand strategy, product craft, and technical implementation." | `en.json` hero.tagline2 |
| Overline | "Design Director / Design Systems & Developer Experience / Design Engineering" | `en.json` hero.tagline |
| Version badge | "V2.0.01" | D-02 (static) |
| Tagline right | "Built for human and AI assistance" | D-02 (static) |
| Primary CTA | "Ask about" | D-03 — links to `/llms.txt` |
| Menu input placeholder | "type to filter" | `en.json` menu.inputHint.keyboard |
| Navigation hints (keyboard) | "Esc to go back / arrows to navigate / Enter to select" | `en.json` menu.navigation.keyboard |
| Data-stamp format | `PRJ_YEAR // NNN` (e.g., "PRJ_2024 // 001") | D-20 |
| Tech tags | "Next.js", "React", "Tailwind", "Vercel" | D-22, D-26 (static hardcoded list) |
| Copyright | "(c) 2026 Caio Ogata" | Standard |
| Contact button | "Contact" | D-22 |
| Form submit | "Send message" | Default (Claude's discretion per D-25) |
| Form — name label | "Name" | Standard |
| Form — email label | "Email" | Standard |
| Form — message label | "Message" | Standard |
| Form success | "Message sent. I'll get back to you soon." | Default |
| Form error | "Failed to send. Try again or email me directly." | Default |
| Empty state | Not applicable — home page always has content | N/A |
| Destructive actions | None in this phase | N/A |

---

## Responsive Behavior

| Section | Desktop (>=1024px) | Tablet (>=768px) | Mobile (<768px) |
|---------|-------------------|------------------|-----------------|
| Intro | Full viewport height, 72px headline | Full width, 48px headline | Full width, 36px headline |
| Sticky header | Full width, logo + version + tagline + CTA | Logo + CTA only | Logo + CTA only |
| Menu | 8-col centered, floating preview on hover | 8-col, no floating preview | 4-col, tap-to-navigate, no preview (D-09) |
| Projects grid | 6-6 (2 columns), 420px card height | 8-col single column, 320px height | 4-col single column, 280px height |
| Footer | 8-col centered, inline expand | 8-col, inline expand | 4-col, full width expand |
| Navigation bar | Keyboard hints visible | Simplified | Touch-optimized, no keyboard hints |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized (out of scope per REQUIREMENTS.md) |

No third-party registries declared. No safety vetting required.

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|---------------|
| Keyboard navigation | Full arrow key / Enter / Esc support on menu (D-08, D-13) |
| Focus visibility | Brand yellow outline on focus-visible for interactive elements |
| Skip link | Already in globals.css (`.sr-only` pattern) |
| Color contrast | AA minimum — tertiary text restricted to 18px+ (semantic.css annotation) |
| Touch targets | 44px minimum on all interactive elements (buttons, menu rows on mobile) |
| Reduced motion | All animations suppressed (existing globals.css rule) |
| Semantic HTML | `<header>`, `<main>`, `<nav>`, `<footer>`, `<section>` with `aria-label` |
| Form labels | All form inputs have visible labels + `aria-required` |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
