---
phase: 04.1-project-gallery-blocks
plan: 01
subsystem: ui
tags: [css-grid, gallery, scroll-reveal, react, typescript]

# Dependency graph
requires:
  - phase: 03-project-pages
    provides: ProjectSection type, GalleryRow interface, useScrollReveal hook
provides:
  - Span-aware staggered gallery component (grid-cols-12 per row)
  - Extended ProjectSection type with gallery-full-bleed and gallery-stick
  - bgImage/fgImage/bgAlt/fgAlt fields for full-bleed gallery data model
affects: [04.1-02-PLAN, project-pages]

# Tech tracking
tech-stack:
  added: []
  patterns: [static-class-maps-for-responsive-spans, stagger-index-scroll-offset]

key-files:
  created: []
  modified:
    - src/content/types.ts
    - src/components/sections/v2/project/ProjectGalleryStaggered.tsx

key-decisions:
  - "Stagger reveal via startFraction offset (0.03 per image index) rather than CSS animation-delay"
  - "Defensive fallback: Math.floor(12/images.length) when spans[i] undefined"

patterns-established:
  - "Static class maps (DESKTOP_SPAN, TABLET_SPAN, MOBILE_SPAN) for responsive grid spans without dynamic template literals"
  - "Sub-component pattern with staggerIndex prop to avoid hooks-in-loops while enabling per-image scroll offset"

requirements-completed: [GAL-01]

# Metrics
duration: 2min
completed: 2026-04-21
---

# Phase 04.1 Plan 01: Gallery Staggered Rewrite Summary

**Span-aware CSS grid gallery replacing broken zigzag pattern, with extended type system for full-bleed and sticky gallery types**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-21T17:43:27Z
- **Completed:** 2026-04-21T17:45:01Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Rewrote ProjectGalleryStaggered to render rows as 12-col CSS grids respecting spans[] from JSON data (12, 6-6, 4-4-4, 8-4 patterns)
- Extended ProjectSection type union with gallery-full-bleed and gallery-stick, plus bgImage/fgImage/bgAlt/fgAlt fields
- Replaced fixed 340px heights with natural aspect ratio (500px max, 200px min) and sharp corners (0px radius)
- Added staggered scroll reveal timing via startFraction offset per image index

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend ProjectSection type for all gallery types** - `20a2cb8` (feat)
2. **Task 2: Rewrite ProjectGalleryStaggered for span-aware row grid** - `077afa9` (feat)

## Files Created/Modified
- `src/content/types.ts` - Added gallery-full-bleed/gallery-stick to type union, bgImage/fgImage/bgAlt/fgAlt fields
- `src/components/sections/v2/project/ProjectGalleryStaggered.tsx` - Complete rewrite: span-aware row grid, static class maps, stagger reveal, sharp corners, tight gaps

## Decisions Made
- Stagger reveal uses startFraction offset (0.03 per image) in useScrollReveal rather than CSS animation-delay -- integrates with existing scroll-linked system
- Defensive fallback calculates equal spans when spans array is shorter than images array, with dev-mode console.warn

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Known Stubs
None - component is fully functional with existing en.json data.

## Next Phase Readiness
- Type system ready for Plan 02 to implement gallery-full-bleed and gallery-stick components
- ProjectSection type union and data model fields are in place
- Staggered gallery backward-compatible with all existing project data

---
*Phase: 04.1-project-gallery-blocks*
*Completed: 2026-04-21*
