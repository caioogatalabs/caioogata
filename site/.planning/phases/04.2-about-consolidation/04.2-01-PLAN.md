---
phase: 04.2-about-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/v2/about/SectionDivider.tsx
  - src/content/en.json
  - src/content/pt-br.json
  - src/content/types.ts
autonomous: true
requirements:
  - ABT-01
  - ABT-05

must_haves:
  truths:
    - "A reusable SectionDivider component renders a horizontal rule with a left-aligned mono section code + label"
    - "Home menu shows only 5 entries (about, projects, experience, philosophy, contact) in both en.json and pt-br.json"
    - "clients.shortDescription field exists in both locales (EN + PT-BR) without removing the existing description"
    - "Content.clients type unconditionally exposes shortDescription as an optional field"
    - "MenuSection.tsx contains no hardcoded index references that would break when items array length shrinks from 8 to 5"
  artifacts:
    - path: "src/components/sections/v2/about/SectionDivider.tsx"
      provides: "Reusable divider atom with code+label pair, used by all four about subsections"
      contains: "export function SectionDivider"
    - path: "src/content/en.json"
      provides: "Trimmed menu.items (5 entries) + new clients.shortDescription"
      contains: "shortDescription"
    - path: "src/content/pt-br.json"
      provides: "Parallel trimmed menu + parallel shortDescription"
      contains: "shortDescription"
    - path: "src/content/types.ts"
      provides: "Content.clients with shortDescription?: string"
      contains: "shortDescription"
  key_links:
    - from: "src/components/sections/v2/about/SectionDivider.tsx"
      to: "src/tokens/semantic.css"
      via: "Tailwind utility classes (border-border-secondary, text-text-tertiary)"
      pattern: "border-border-secondary|text-text-tertiary"
    - from: "src/content/en.json (menu.items)"
      to: "src/components/sections/v2/MenuSection.tsx"
      via: "MenuSection imports content.menu.items"
      pattern: "menu\\.items"
---

<objective>
Create the foundational SectionDivider atom (covers ABT-01) and trim the home menu while adding the `clients.shortDescription` content field (covers ABT-05 and unblocks ABT-03).

Purpose: All four about subsections (BioBlock, SkillsBlock, ClientsBlock, EducationBlock) use SectionDivider — it must exist before Wave 2. The menu trim and shortDescription addition share the same JSON files, so they're bundled here to avoid Wave 2/3 plans touching content files. The `Content.clients` type is also extended unconditionally so Wave 2 ClientsBlock can read `shortDescription` cleanly without defensive fallbacks.

Output: One new component file + edits to two content JSON files + an unconditional type extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.2-about-consolidation/04.2-CONTEXT.md
@CLAUDE.md
@src/components/sections/v2/IntroSection.tsx
@src/tokens/semantic.css

<interfaces>
<!-- Welcome Bar typography precedent (mono, small, uppercase, text-text-tertiary) — divider mirrors this. -->
<!-- From CLAUDE.md "Intro / Hero Section": welcome bar uses Fabio XM Semibold 12px. -->
<!-- Divider uses Cascadia Mono (font-mono class) per CONTEXT D-05 + D-06. -->

Tailwind tokens used (from semantic.css):
- text-text-tertiary  → low-contrast label text
- border-border-secondary → divider rule color
- font-mono → Cascadia Mono
- text-xs → 12px

Container padding precedent (CLAUDE.md §Component Patterns):
- Full-width sections use direct `px-5 md:px-8 lg:px-16` padding (no Grid wrapper required when alignment is left-edge).

en.json content shapes touched:
```json
{
  "menu": { "items": [{ "key": "...", "label": "...", "description": "..." }, ...] },
  "clients": { "command": "...", "heading": "Notable Clients", "description": "...long...", "list": ["..."] }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create SectionDivider atom</name>
  <files>src/components/sections/v2/about/SectionDivider.tsx</files>
  <read_first>
    - .planning/phases/04.2-about-consolidation/04.2-CONTEXT.md (D-04, D-05, D-06)
    - CLAUDE.md (Component Patterns + Animation System sections)
    - src/components/sections/v2/IntroSection.tsx (welcome bar typography precedent)
    - src/tokens/semantic.css (verify text-text-tertiary and border-border-secondary tokens exist)
  </read_first>
  <action>
    Create `src/components/sections/v2/about/SectionDivider.tsx` (mkdir -p the `about/` directory first).

    Implements D-04 + D-05 + D-06 from CONTEXT.md:

    ```tsx
    'use client'

    interface SectionDividerProps {
      /** Section code, e.g. "1.1" */
      code: string
      /** Section label, e.g. "Bio" */
      label: string
    }

    /**
     * Horizontal rule divider with left-aligned section code + label.
     * Used to introduce numbered subsections inside /about (1.1 / Bio, 1.2 / Skills, etc).
     * Typography mirrors the IntroSection Welcome Bar (mono, small, uppercase, text-text-tertiary).
     */
    export function SectionDivider({ code, label }: SectionDividerProps) {
      return (
        <div className="px-5 md:px-8 lg:px-16">
          <div className="border-t border-border-secondary pt-4 pb-2">
            <span className="font-mono text-xs uppercase tracking-[0.88px] text-text-tertiary">
              {code} / {label}
            </span>
          </div>
        </div>
      )
    }
    ```

    Concrete values used (per D-05/D-06):
    - Padding: `px-5 md:px-8 lg:px-16` (matches full-width section pattern from CLAUDE.md)
    - Rule: `border-t border-border-secondary` (D-06 token for rule)
    - Label color: `text-text-tertiary` (D-06 token for label)
    - Font: `font-mono text-xs uppercase tracking-[0.88px]` (D-05 — matches Welcome Bar)
    - Vertical rhythm: `pt-4 pb-2` (compact — divider is a separator, not a section header)
    - Format string: `{code} / {label}` (e.g. "1.1 / Bio") — note the spaces around the slash, per CONTEXT examples.

    Do NOT add `useInView` or `-entrance` classes here — the divider is purely structural; entrance animation lives on the block content below it (D-29).
    Do NOT use raw hex/oklch — semantic tokens only.
    Do NOT import from any V1 path.
  </action>
  <verify>
    <automated>test -f src/components/sections/v2/about/SectionDivider.tsx && grep -q "export function SectionDivider" src/components/sections/v2/about/SectionDivider.tsx && grep -q "border-border-secondary" src/components/sections/v2/about/SectionDivider.tsx && grep -q "text-text-tertiary" src/components/sections/v2/about/SectionDivider.tsx && grep -q "font-mono" src/components/sections/v2/about/SectionDivider.tsx && grep -q "{code} / {label}" src/components/sections/v2/about/SectionDivider.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/sections/v2/about/SectionDivider.tsx` exists
    - `grep -q "export function SectionDivider" src/components/sections/v2/about/SectionDivider.tsx` returns 0
    - `grep -q "code: string" src/components/sections/v2/about/SectionDivider.tsx` returns 0 (typed prop)
    - `grep -q "label: string" src/components/sections/v2/about/SectionDivider.tsx` returns 0 (typed prop)
    - `grep -q "border-border-secondary" src/components/sections/v2/about/SectionDivider.tsx` returns 0
    - `grep -q "text-text-tertiary" src/components/sections/v2/about/SectionDivider.tsx` returns 0
    - `grep -q "font-mono" src/components/sections/v2/about/SectionDivider.tsx` returns 0
    - `grep -q "px-5 md:px-8 lg:px-16" src/components/sections/v2/about/SectionDivider.tsx` returns 0
    - `grep -E "(#|oklch\\()" src/components/sections/v2/about/SectionDivider.tsx` returns no matches (no raw color values)
    - File does not import from `@/components/sections/[A-Z]` (V1 path) — `grep -E "from '@/components/sections/[A-Z]" src/components/sections/v2/about/SectionDivider.tsx` returns no matches
  </acceptance_criteria>
  <done>
    SectionDivider component exists, is exported, uses semantic tokens only, matches Welcome Bar typography (mono / xs / uppercase / tracking-[0.88px] / text-text-tertiary), and has no V1 dependencies. The atom is ready to be consumed by all four about block components in Wave 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Trim menu.items in en.json + pt-br.json, add clients.shortDescription to both locales, and extend Content.clients type</name>
  <files>src/content/en.json, src/content/pt-br.json, src/content/types.ts</files>
  <read_first>
    - .planning/phases/04.2-about-consolidation/04.2-CONTEXT.md (D-03, D-14, D-27)
    - src/content/en.json (specifically lines 32-73 for menu.items, lines around 1706-1710 for clients block)
    - src/content/pt-br.json (parallel structure: menu.items around line 32, clients around line 986)
    - src/content/types.ts (Menu and clients shape — confirm `description` is typed; `shortDescription` will be added)
    - src/components/sections/v2/MenuSection.tsx (verify-only — confirm MenuSection consumes `content.items` via the `useMenuNavigation` hook with NO hardcoded index references like `items[5]` or `items.slice(0, 8)`. Do NOT modify this file.)
  </read_first>
  <action>
    Three-part edit; both JSON files must remain valid JSON and `types.ts` must compile after the changes.

    Part A — Trim menu.items (D-03 / ABT-05) in BOTH en.json and pt-br.json:
    Remove the three menu entries with `"key": "skills"`, `"key": "education"`, and `"key": "clients"`. Keep the remaining 5 entries in this exact order: about, projects, experience, philosophy, contact.

    For en.json, the `menu.items` array currently has 8 items (lines 32-73). After the edit it should have exactly 5 items in this order:
    1. `{ "key": "about", "label": "about", "description": "Bio, expertise, and background." }`
    2. `{ "key": "projects", "label": "projects", "description": "Selected design and product work." }`
    3. `{ "key": "experience", "label": "experience", "description": "Roles, companies, and key achievements." }`
    4. `{ "key": "philosophy", "label": "philosophy", "description": "Design principles and how I approach product and teams." }`
    5. `{ "key": "contact", "label": "contact", "description": "How to get in touch and connect." }`

    For pt-br.json, perform the parallel removal (preserve the existing PT-BR `label` and `description` text on the 5 surviving items — DO NOT translate them yourself, just keep what's already there).

    Part B — Add `clients.shortDescription` (D-14 / D-27) to BOTH locales:
    The existing `clients.description` field in en.json (line 1709) and pt-br.json (line 989) MUST be preserved unchanged (V1 still consumes it).
    Add a new sibling field `shortDescription` to the `clients` object in both files.

    EN value (Claude's discretion per CONTEXT — keep one sentence, scoped to "two decades + brands across Brazil and beyond"):
    ```
    "shortDescription": "Two decades of work for brands across Brazil and beyond."
    ```

    PT-BR value (translated to match existing PT-BR voice in the file — clients.description is already translated, so shortDescription should be too):
    ```
    "shortDescription": "Duas décadas de trabalho para marcas no Brasil e em diversos países."
    ```

    Place `shortDescription` directly AFTER `description` and BEFORE `list` inside the `clients` object in both files, so the resulting shape is:
    ```json
    "clients": {
      "command": "...",
      "heading": "...",
      "description": "...long...",
      "shortDescription": "...short...",
      "list": [...]
    }
    ```

    Part C — Extend Content.clients type UNCONDITIONALLY:
    Edit `src/content/types.ts`. Locate the `Content.clients` interface block (around lines 251-256, currently `{ command, heading, description, list }`) and add `shortDescription?: string` after `description`. The optional `?` keeps the type definition tolerant of any partial JSON during V1 builds, but Wave 2 plans MUST assume the field is present (since both en.json and pt-br.json now contain it after Part B).

    Resulting `Content.clients` interface shape:
    ```typescript
    clients: {
      command: string
      heading: string
      description: string
      shortDescription?: string
      list: string[]
    }
    ```

    Do NOT touch any other field on the type, do not reformat unchanged JSON. Use targeted Edit operations, not full-file rewrites.

    Validate both JSON files are still parseable after the edit:
    ```bash
    node -e "JSON.parse(require('fs').readFileSync('src/content/en.json','utf8'))"
    node -e "JSON.parse(require('fs').readFileSync('src/content/pt-br.json','utf8'))"
    ```

    Validate the TypeScript still compiles after the type extension:
    ```bash
    pnpm tsc --noEmit
    ```

    Part D — VERIFY-ONLY guard against MenuSection.tsx hardcoded indices (covers W4):
    Run the following grep against `src/components/sections/v2/MenuSection.tsx` to confirm there is NO hardcoded `items[N]` access for indices 5/6/7 and NO `items.slice(0, N)` truncation that would break when the array shrinks from 8 to 5 items:
    ```bash
    ! grep -nE "items\\[5\\]|items\\[6\\]|items\\[7\\]|items\\.slice\\(0,[[:space:]]*[678]\\)" src/components/sections/v2/MenuSection.tsx
    ```
    This is a SAFETY CHECK — if the grep finds matches, STOP the task and report the issue. The current MenuSection.tsx (read in `read_first`) iterates `filteredItems.map((item, index) => ...)` from `useMenuNavigation`, which is safe — the array length is whatever the JSON provides. But the safety check guarantees no future regression.
    DO NOT modify MenuSection.tsx in this task.
  </action>
  <verify>
    <automated>node -e "const en=JSON.parse(require('fs').readFileSync('src/content/en.json','utf8'));const pt=JSON.parse(require('fs').readFileSync('src/content/pt-br.json','utf8'));const enKeys=en.menu.items.map(i=>i.key);const ptKeys=pt.menu.items.map(i=>i.key);const expected=['about','projects','experience','philosophy','contact'];if(JSON.stringify(enKeys)!==JSON.stringify(expected))throw new Error('en menu keys mismatch: '+JSON.stringify(enKeys));if(JSON.stringify(ptKeys)!==JSON.stringify(expected))throw new Error('pt menu keys mismatch: '+JSON.stringify(ptKeys));if(typeof en.clients.shortDescription!=='string'||en.clients.shortDescription.length===0)throw new Error('en shortDescription missing');if(typeof pt.clients.shortDescription!=='string'||pt.clients.shortDescription.length===0)throw new Error('pt shortDescription missing');if(typeof en.clients.description!=='string'||en.clients.description.length===0)throw new Error('en description regression');if(typeof pt.clients.description!=='string'||pt.clients.description.length===0)throw new Error('pt description regression');console.log('OK')" && grep -q "shortDescription" src/content/types.ts && ! grep -nE "items\[5\]|items\[6\]|items\[7\]|items\.slice\(0,[[:space:]]*[678]\)" src/components/sections/v2/MenuSection.tsx && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('src/content/en.json','utf8')).menu.items.length)"` outputs `5`
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('src/content/pt-br.json','utf8')).menu.items.length)"` outputs `5`
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('src/content/en.json','utf8')).menu.items.map(i=>i.key).join(','))"` outputs `about,projects,experience,philosophy,contact`
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('src/content/pt-br.json','utf8')).menu.items.map(i=>i.key).join(','))"` outputs `about,projects,experience,philosophy,contact`
    - `grep -c '"key": "skills"' src/content/en.json` returns `0`
    - `grep -c '"key": "education"' src/content/en.json` returns `0`
    - `grep -c '"key": "clients"' src/content/en.json` returns `0`
    - `grep -c '"key": "skills"' src/content/pt-br.json` returns `0`
    - `grep -c '"key": "education"' src/content/pt-br.json` returns `0`
    - `grep -c '"key": "clients"' src/content/pt-br.json` returns `0`
    - `grep -c '"shortDescription"' src/content/en.json` returns `1`
    - `grep -c '"shortDescription"' src/content/pt-br.json` returns `1`
    - `grep -c '"description":' src/content/en.json | xargs test 1 -le` (description still present multiple times)
    - `node -e "const c=JSON.parse(require('fs').readFileSync('src/content/en.json','utf8')).clients; if(c.description.length<200)throw new Error('long description was truncated')"`
    - Both files are valid JSON: `node -e "JSON.parse(require('fs').readFileSync('src/content/en.json','utf8'))"` exits 0
    - Both files are valid JSON: `node -e "JSON.parse(require('fs').readFileSync('src/content/pt-br.json','utf8'))"` exits 0
    - `Content.clients` type extended: `grep -E "shortDescription\\?: string" src/content/types.ts` returns 1 match
    - `pnpm tsc --noEmit` exits 0 (the type addition compiles cleanly)
    - MenuSection.tsx has no hardcoded indices (W4 guard): `! grep -nE "items\\[5\\]|items\\[6\\]|items\\[7\\]|items\\.slice\\(0,[[:space:]]*[678]\\)" src/components/sections/v2/MenuSection.tsx` exits 0
    - V1 untouched: `git diff --name-only src/components/sections/Clients.tsx src/components/sections/Education.tsx src/components/sections/Skills.tsx 2>/dev/null` returns empty
    - MenuSection.tsx NOT modified by this task: `git diff --name-only src/components/sections/v2/MenuSection.tsx` returns empty
  </acceptance_criteria>
  <done>
    Both content JSON files are valid; menu has exactly 5 keys in canonical order; `clients.shortDescription` exists in both locales as a non-empty single sentence; `clients.description` (long version) and `clients.list` are preserved; `Content.clients` in `types.ts` now includes `shortDescription?: string`; MenuSection.tsx is verified to have no hardcoded indices and is left untouched. Wave 2 plans can now read `content.clients.shortDescription` directly without defensive fallback chains.
  </done>
</task>

</tasks>

<verification>
- All Task 1 acceptance criteria pass.
- All Task 2 acceptance criteria pass.
- `pnpm tsc --noEmit` exits 0 (TypeScript compiles with the unconditional `shortDescription?: string` extension).
- `git diff --stat` shows ONLY four target files (`src/components/sections/v2/about/SectionDivider.tsx`, `src/content/en.json`, `src/content/pt-br.json`, `src/content/types.ts`).
</verification>

<success_criteria>
1. `src/components/sections/v2/about/SectionDivider.tsx` exists, exports `SectionDivider({ code, label })`, uses only semantic tokens, has no V1 imports, and matches IntroSection Welcome Bar typography (font-mono, text-xs, uppercase, tracking-[0.88px], text-text-tertiary).
2. The home menu in BOTH `src/content/en.json` and `src/content/pt-br.json` contains exactly 5 entries in this order: about, projects, experience, philosophy, contact.
3. `clients.shortDescription` is present in both locales as a non-empty string sibling of `clients.description`; `clients.description` and `clients.list` are unchanged.
4. Both JSON files remain valid JSON.
5. `Content.clients` in `src/content/types.ts` includes `shortDescription?: string` (unconditional, optional for type tolerance).
6. `MenuSection.tsx` is left untouched and verified to have no hardcoded item indices.
7. No V1 component file (`src/components/sections/Skills.tsx`, `Education.tsx`, `Clients.tsx`, etc.) is touched.
8. `pnpm tsc --noEmit` and `pnpm build` still succeed.
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-about-consolidation/04.2-01-SUMMARY.md` documenting:
- The SectionDivider API (`code: string`, `label: string`) for Wave 2 plans to consume.
- The exact menu item order after the trim.
- The exact EN and PT-BR strings used for `clients.shortDescription` (so Wave 2 ClientsBlock plan can quote them in tests).
- Confirmation that `Content.clients` in `types.ts` was extended with `shortDescription?: string` (UNCONDITIONAL — Wave 2 can rely on it).
- Confirmation that `MenuSection.tsx` was NOT modified and has no hardcoded indices.
</output>
</output>
