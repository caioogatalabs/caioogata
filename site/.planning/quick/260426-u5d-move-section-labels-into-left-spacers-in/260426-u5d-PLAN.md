---
phase: 260426-u5d
plan: quick
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/v2/AboutSection.tsx
  - src/components/sections/v2/about/AboutPinned.tsx
  - src/components/sections/v2/about/BioBlock.tsx
  - src/components/sections/v2/about/SkillsBlock.tsx
  - src/components/sections/v2/about/ClientsBlock.tsx
  - src/components/sections/v2/about/EducationBlock.tsx
autonomous: true
requirements:
  - QU-260426-u5d-A  # Section labels move into left spacer column
  - QU-260426-u5d-B  # Display paragraph text-indent (8em) on AboutPinned + BioBlock final quote
  - QU-260426-u5d-C  # AboutSection header band gets bottom padding (pb-16/24/32)
must_haves:
  truths:
    - "Each of the 4 about-blocks (Bio, Skills, Clients, Education) renders its `${code} / ${label}` pill INSIDE the left spacer column — no horizontal divider above the block."
    - "The 1.1 / Bio, 1.2 / Skills, 1.3 / Notable Clients, 1.4 / Education labels are typographically identical to the welcome-bar pattern (font-mono text-xs uppercase tracking-[0.88px] text-text-tertiary)."
    - "AboutPinned's first paragraph is text-indented by 8em on its first line."
    - "BioBlock's full-width final quote is text-indented by 8em on its first line."
    - "The dark `bg-bg-surface-secondary` band at the top of /about extends well below the sticky pills row (pb-16 mobile / pb-24 tablet / pb-32 desktop)."
    - "SectionDivider.tsx still exists on disk but is no longer imported anywhere in the 4 about blocks."
    - "SkillsBlock accordion behaviour (hover open / click toggle / mouseLeave collapse / per-skill yellow level bar) is unchanged."
    - "All -entrance -slide-up animations and useInView refs in the 4 blocks are preserved."
  artifacts:
    - path: "src/components/sections/v2/AboutSection.tsx"
      provides: "Header band wrapper with extended bottom padding"
      contains: "pb-16 md:pb-24 lg:pb-32"
    - path: "src/components/sections/v2/about/AboutPinned.tsx"
      provides: "First-paragraph text-indent"
      contains: "textIndent: '8em'"
    - path: "src/components/sections/v2/about/BioBlock.tsx"
      provides: "Spacer-column label + final-quote text-indent, no SectionDivider import"
      contains: "1.1 / Bio"
    - path: "src/components/sections/v2/about/SkillsBlock.tsx"
      provides: "Spacer-column label, no SectionDivider import"
      contains: "1.2 / Skills"
    - path: "src/components/sections/v2/about/ClientsBlock.tsx"
      provides: "Spacer-column label, no SectionDivider import"
      contains: "1.3 / Notable Clients"
    - path: "src/components/sections/v2/about/EducationBlock.tsx"
      provides: "Spacer-column label, no SectionDivider import"
      contains: "1.4 / Education"
  key_links:
    - from: "src/components/sections/v2/about/{Bio,Skills,Clients,Education}Block.tsx"
      to: "left spacer GridItem"
      via: "label span as direct child of the GridItem"
      pattern: "font-mono text-xs uppercase tracking-\\[0\\.88px\\] text-text-tertiary"
    - from: "src/components/sections/v2/about/AboutPinned.tsx"
      to: "first-paragraph <p> inline style"
      via: "textIndent property added next to fontSize/lineHeight/letterSpacing"
      pattern: "textIndent: '8em'"
    - from: "src/components/sections/v2/about/BioBlock.tsx"
      to: "final-quote <p> inline style"
      via: "textIndent property added next to fontSize/lineHeight/letterSpacing"
      pattern: "textIndent: '8em'"
---

<objective>
Three Figma 701:303 alignment refinements on /about V2 packaged as one quick task:

  - **A.** Move the four `${code} / ${label}` section labels (1.1 / Bio, 1.2 / Skills,
        1.3 / Notable Clients, 1.4 / Education) out of horizontal `<SectionDivider/>` rules
        and into the existing left spacer GridItem of each block. SectionDivider.tsx stays on
        disk; we just stop importing it from the 4 blocks.
  - **B.** Add `textIndent: '8em'` to the two display paragraphs (AboutPinned first paragraph
        + BioBlock full-width final quote).
  - **C.** Extend AboutSection's `bg-bg-surface-secondary` header band downward by adding
        `pb-16 md:pb-24 lg:pb-32` to the wrapper around `<StickyLogoBar/>`.

Purpose: tightens /about composition to match Figma 701:303 — labels now sit alongside the
content they describe (left spacer, top-aligned), display paragraphs gain magazine-style
first-line indent, and the dark hero band properly frames the sticky pills.

Output: 6 files modified, 1 single final commit. SectionDivider.tsx file untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/components/sections/v2/AboutSection.tsx
@src/components/sections/v2/about/AboutPinned.tsx
@src/components/sections/v2/about/BioBlock.tsx
@src/components/sections/v2/about/SkillsBlock.tsx
@src/components/sections/v2/about/ClientsBlock.tsx
@src/components/sections/v2/about/EducationBlock.tsx
@src/components/sections/v2/about/SectionDivider.tsx
@src/components/layout/Grid.tsx

<interfaces>
<!-- Verified-from-codebase contracts. Use these directly — no exploration needed. -->

From src/components/layout/Grid.tsx:
```typescript
interface GridItemProps extends HTMLAttributes<HTMLDivElement> {
  span?: number       // desktop span (out of 12)
  tabletSpan?: number // tablet span (out of 8)
  mobileSpan?: number // mobile span (out of 4)
  children?: ReactNode
}
// Static class maps: MOBILE_SPAN, TABLET_SPAN, DESKTOP_SPAN — DO NOT use dynamic Tailwind.
// Self-closing GridItems are valid (no children); convert to <GridItem ...>...</GridItem>
// only when adding the label span.
```

From src/components/sections/v2/about/SectionDivider.tsx (canonical label markup — REUSE THE STRINGS):
```tsx
<span className="font-mono text-xs uppercase tracking-[0.88px] text-text-tertiary">
  {code} / {label}
</span>
```

Current spacer pattern in BioBlock / SkillsBlock / EducationBlock (after quick task 260426-sjr):
```tsx
<Grid className="!px-0">
  {/* Spacer cols 1-4 — currently empty / self-closing */}
  <GridItem span={4} tabletSpan={2} mobileSpan={4} />

  {/* Content cols 5-12 */}
  <GridItem span={8} tabletSpan={6} mobileSpan={4} className="lg:col-start-5">
    {/* ... block content ... */}
  </GridItem>
</Grid>
```

Current spacer pattern in ClientsBlock (cols 6-6, not 4-8):
```tsx
<Grid className="!px-0 mb-12 md:mb-16">
  <GridItem span={6} tabletSpan={2} mobileSpan={4} />
  <GridItem span={6} tabletSpan={6} mobileSpan={4}>
    {/* ... description paragraph ... */}
  </GridItem>
</Grid>
```
</interfaces>

<verified_facts>
<!-- Read directly from disk during planning. -->
1. BioBlock.tsx ALREADY has the spacer-cols-1-4 + content-cols-5-12 layout
   (introduced in quick task 260426-sjr). No structural restructure required —
   only delete the `<SectionDivider>` call + its import, and add the label span
   inside the existing self-closing spacer GridItem (which becomes opening/closing).
2. SkillsBlock.tsx and EducationBlock.tsx use IDENTICAL spacer markup
   (`<GridItem span={4} tabletSpan={2} mobileSpan={4} />`, self-closing).
3. ClientsBlock.tsx's spacer is `span={6} tabletSpan={2} mobileSpan={4}` (cols 6-6,
   not 4-8 like the others) and is already self-closing. The label still goes inside it.
4. SectionDivider.tsx is imported in all 4 blocks today via
   `import { SectionDivider } from './SectionDivider'`. After this task, that import
   line is removed from all 4 blocks. The file itself stays — DO NOT delete it.
5. AboutPinned.tsx's first paragraph already has an inline-style object with
   fontFamily / fontSize: '3rem' / lineHeight / letterSpacing / fontWeight / opacity /
   transition. Just add `textIndent: '8em'` as another property on that object.
6. BioBlock.tsx's final quote `<p>` (rendered when `finalQuote` is truthy) has the
   identical inline-style shape. Same one-line addition.
7. AboutSection.tsx's header wrapper today is exactly:
   `<div className="bg-bg-surface-secondary pt-8 md:pt-10 lg:pt-12">`.
   The change is appending ` pb-16 md:pb-24 lg:pb-32` inside the same className string.
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Changes A + B + C across the 6 files (single commit at the end)</name>
  <files>
    src/components/sections/v2/AboutSection.tsx,
    src/components/sections/v2/about/AboutPinned.tsx,
    src/components/sections/v2/about/BioBlock.tsx,
    src/components/sections/v2/about/SkillsBlock.tsx,
    src/components/sections/v2/about/ClientsBlock.tsx,
    src/components/sections/v2/about/EducationBlock.tsx
  </files>

  <action>
  Make the following exact edits. **Do NOT auto-commit after each file.** All 6 files
  ship in a SINGLE FINAL COMMIT (the user authorized one commit). After all files are
  edited, run typecheck, then commit once with a descriptive message.

  ────────────────────────────────────────────────────────────────────────────
  CHANGE C (do first — it's the smallest, highest-up file in the page):
  ────────────────────────────────────────────────────────────────────────────

  **File: src/components/sections/v2/AboutSection.tsx**
  - Locate the existing wrapper div (currently line 29):
    ```tsx
    <div className="bg-bg-surface-secondary pt-8 md:pt-10 lg:pt-12">
    ```
  - Replace with:
    ```tsx
    <div className="bg-bg-surface-secondary pt-8 md:pt-10 lg:pt-12 pb-16 md:pb-24 lg:pb-32">
    ```
  - That is the only change in this file. Do not touch the comment above, do not touch
    `<StickyLogoBar />`, do not touch any other section.

  ────────────────────────────────────────────────────────────────────────────
  CHANGE B (one-line additions on two display paragraphs):
  ────────────────────────────────────────────────────────────────────────────

  **File: src/components/sections/v2/about/AboutPinned.tsx**
  - Locate the first paragraph `<p>` (inside the cols 1-12 GridItem, around lines
    100-113). Its `style={{ ... }}` object today contains:
    `fontFamily, fontSize: '3rem', lineHeight: 1.15, letterSpacing: '-0.96px',
     fontWeight: 400, opacity, transition`.
  - Add ONE new property to that style object: `textIndent: '8em'`. Put it directly
    after `fontWeight: 400,` and before `opacity:`. Keep all other properties verbatim.
  - Resulting style block (for reference — preserve exact existing values):
    ```tsx
    style={{
      fontFamily: 'var(--font-sans)',
      fontSize: '3rem',
      lineHeight: 1.15,
      letterSpacing: '-0.96px',
      fontWeight: 400,
      textIndent: '8em',
      opacity: paragraphOpacity,
      transition: reducedMotion ? 'none' : 'opacity 60ms linear',
    }}
    ```
  - That is the only change in this file. Do NOT touch the image-translate logic,
    the useScrollVideo hook, the reduced-motion handling, or any constants.

  **File: src/components/sections/v2/about/BioBlock.tsx — Change-B portion**
  - Locate the final-quote `<p>` (rendered inside `{finalQuote && (...)}` block,
    around lines 90-103). Its `style={{ ... }}` object today contains:
    `fontFamily, fontSize: '3rem', lineHeight: 1.15, letterSpacing: '-0.96px',
     fontWeight: 400`.
  - Add ONE new property to that style object: `textIndent: '8em'`. Append it as the
    last property (after `fontWeight: 400,`). Keep the existing `mt-16 md:mt-20` and
    `-entrance -slide-up -a-${...}` className intact.

  ────────────────────────────────────────────────────────────────────────────
  CHANGE A (the big repeating one — labels into spacers, divider gone):
  ────────────────────────────────────────────────────────────────────────────

  Apply the SAME 4-step recipe to each of the 4 block files. Recipe:

  STEP 1. Delete the import: remove the line
            `import { SectionDivider } from './SectionDivider'`
  STEP 2. Delete the JSX call: remove the `<SectionDivider code="X.Y" label="..." />`
          line that sits as the first child of the outer `<div>` returned by the block.
          Keep the outer `<div>` itself.
  STEP 3. Convert the existing self-closing spacer GridItem into opening/closing form.
  STEP 4. Inside the spacer GridItem, render a single `<span>` with the canonical
          mono-label markup. The span sits at the top of the spacer naturally because
          it's the only child (no flex/justify needed — block flow puts it at top-left).

  Canonical label markup (copy verbatim, swap `${code}` and `${label}`):
  ```tsx
  <span
    className="font-mono text-xs uppercase tracking-[0.88px] text-text-tertiary"
  >
    {`${code} / ${label}`}
  </span>
  ```
  (`${code}` and `${label}` are written as static strings in each block — see per-file
  values below — there are no props.)

  Per-file specifics:

  **File: src/components/sections/v2/about/BioBlock.tsx — Change-A portion**
  - Code: `1.1`, label: `Bio`.
  - Remove `import { SectionDivider } from './SectionDivider'` (line 5).
  - Remove `<SectionDivider code="1.1" label="Bio" />` (line 36, first child of outer div).
  - The current spacer (line 44) is `<GridItem span={4} tabletSpan={2} mobileSpan={4} />`.
    Convert to:
    ```tsx
    <GridItem span={4} tabletSpan={2} mobileSpan={4}>
      <span className="font-mono text-xs uppercase tracking-[0.88px] text-text-tertiary">
        1.1 / Bio
      </span>
    </GridItem>
    ```
  - Leave everything else (Core Expertise list, middle paragraphs map, final quote `<p>`
    with the new textIndent from Change B, the contentRef useInView, `-entrance` classes,
    px/py wrapper) untouched.

  **File: src/components/sections/v2/about/SkillsBlock.tsx**
  - Code: `1.2`, label: `Skills`.
  - Remove `import { SectionDivider } from './SectionDivider'` (line 6).
  - Remove `<SectionDivider code="1.2" label="Skills" />` (line 43).
  - Current spacer (line 55) is `<GridItem span={4} tabletSpan={2} mobileSpan={4} />`.
    Convert to:
    ```tsx
    <GridItem span={4} tabletSpan={2} mobileSpan={4}>
      <span className="font-mono text-xs uppercase tracking-[0.88px] text-text-tertiary">
        1.2 / Skills
      </span>
    </GridItem>
    ```
  - Do NOT touch the accordion logic (`expandedCategoryTitle`, `hoveredSkill`,
    `LEVEL_WIDTH`, the per-row mouseEnter/mouseLeave, the grid-rows-[0fr↔1fr] panel
    transition, or the outer `onMouseLeave`). Do NOT touch the per-skill yellow bar
    or the `text-text-inverse` hover swap.

  **File: src/components/sections/v2/about/ClientsBlock.tsx**
  - Code: `1.3`, label: `Notable Clients`.
  - Remove `import { SectionDivider } from './SectionDivider'` (line 7).
  - Remove `<SectionDivider code="1.3" label="Notable Clients" />` (line 48).
  - Current spacer (line 56) is `<GridItem span={6} tabletSpan={2} mobileSpan={4} />`.
    Note this block uses span={6}, not span={4} — that's intentional and unchanged.
    Convert to:
    ```tsx
    <GridItem span={6} tabletSpan={2} mobileSpan={4}>
      <span className="font-mono text-xs uppercase tracking-[0.88px] text-text-tertiary">
        1.3 / Notable Clients
      </span>
    </GridItem>
    ```
  - Do NOT touch the description paragraph (Fabio XM Bold 36px), the logo grid
    (`grid grid-cols-2 lg:grid-cols-4`), CLIENT_LOGOS, or the `-entrance -fade` per-logo.

  **File: src/components/sections/v2/about/EducationBlock.tsx**
  - Code: `1.4`, label: `Education`.
  - Remove `import { SectionDivider } from './SectionDivider'` (line 6).
  - Remove `<SectionDivider code="1.4" label="Education" />` (line 42).
  - Current spacer (line 50) is `<GridItem span={4} tabletSpan={2} mobileSpan={4} />`.
    Convert to:
    ```tsx
    <GridItem span={4} tabletSpan={2} mobileSpan={4}>
      <span className="font-mono text-xs uppercase tracking-[0.88px] text-text-tertiary">
        1.4 / Education
      </span>
    </GridItem>
    ```
  - Do NOT touch `getSortYear`, `getDisplayYear`, the `useMemo`, the flat education
    rows (`flex gap-5 md:gap-8`), the year stamp `w-[100px]`, or the info stack.

  ────────────────────────────────────────────────────────────────────────────
  PROJECT CONVENTIONS (CLAUDE.md) — must hold after edits:
  ────────────────────────────────────────────────────────────────────────────
  - No dynamic Tailwind class strings introduced (the label className is a static literal).
  - Only semantic tokens used (`text-text-tertiary`, `bg-bg-surface-secondary`).
  - `font-mono` class for the structural mono label (Cascadia Mono).
  - All `-entrance -slide-up`/`-fade` classes and `useInView` refs preserved.
  - SkillsBlock accordion behaviour preserved verbatim.
  - SectionDivider.tsx file is NOT deleted, NOT modified — just no longer imported.

  ────────────────────────────────────────────────────────────────────────────
  COMMIT POLICY:
  ────────────────────────────────────────────────────────────────────────────
  - DO NOT commit per file. DO NOT commit per change.
  - After all 6 files are saved, run `pnpm tsc --noEmit` to confirm typecheck passes.
  - Then create ONE final commit with all 6 files staged together.
  - Suggested commit message:
    `fix(quick-260426-u5d): move section labels into spacers + text-indent display paras + extend hero band`
  </action>

  <verify>
    <automated>
    # 1. Typecheck still passes (no TS regressions from JSX edits).
    pnpm tsc --noEmit

    # 2. SectionDivider import removed from all 4 about blocks.
    !grep -l "from './SectionDivider'" src/components/sections/v2/about/{Bio,Skills,Clients,Education}Block.tsx

    # 3. SectionDivider JSX call removed from all 4 about blocks.
    !grep -l "<SectionDivider" src/components/sections/v2/about/{Bio,Skills,Clients,Education}Block.tsx

    # 4. Each block contains its label string inside the file.
    grep -q "1.1 / Bio"              src/components/sections/v2/about/BioBlock.tsx
    grep -q "1.2 / Skills"           src/components/sections/v2/about/SkillsBlock.tsx
    grep -q "1.3 / Notable Clients"  src/components/sections/v2/about/ClientsBlock.tsx
    grep -q "1.4 / Education"        src/components/sections/v2/about/EducationBlock.tsx

    # 5. text-indent applied on the two display paragraphs.
    grep -q "textIndent: '8em'" src/components/sections/v2/about/AboutPinned.tsx
    grep -q "textIndent: '8em'" src/components/sections/v2/about/BioBlock.tsx

    # 6. Header band wrapper now has bottom padding.
    grep -q "pb-16 md:pb-24 lg:pb-32" src/components/sections/v2/AboutSection.tsx

    # 7. SectionDivider.tsx file still exists (not deleted).
    test -f src/components/sections/v2/about/SectionDivider.tsx
    </automated>
  </verify>

  <done>
  - 6 files modified per the action spec above.
  - `pnpm tsc --noEmit` exits 0.
  - All grep checks in <verify> pass (4 negative assertions for SectionDivider removal,
    4 positive assertions for label strings, 2 positive assertions for textIndent,
    1 positive assertion for header padding, 1 file-existence check for SectionDivider.tsx).
  - SkillsBlock accordion behaviour preserved (visual confirmation deferred to user — not
    required for this autonomous task).
  - One final commit covering all 6 files with the suggested message format.
  </done>
</task>

</tasks>

<verification>
The single Task 1 above is the only verification surface. The 14 grep/test checks
inside `<automated>` cover every must_have artifact and key_link. No additional
phase-level verification is needed for a quick task of this size.
</verification>

<success_criteria>
- 6 files committed in a single commit on the v2 branch.
- `pnpm tsc --noEmit` exits 0.
- Section labels render inside the left spacer column of each of the 4 about blocks
  (`1.1 / Bio`, `1.2 / Skills`, `1.3 / Notable Clients`, `1.4 / Education`).
- No `<SectionDivider/>` JSX calls remain in the 4 about blocks; SectionDivider.tsx
  file is otherwise untouched.
- AboutPinned first paragraph and BioBlock final quote both render with `text-indent: 8em`.
- AboutSection header band has `pb-16 md:pb-24 lg:pb-32` on its wrapper.
- All `-entrance -slide-up` animations and `useInView` refs preserved.
- SkillsBlock accordion (hover open / click toggle / mouseLeave collapse / per-skill bar)
  behaviour preserved verbatim.
</success_criteria>

<output>
After completion, this quick task does NOT produce a SUMMARY.md (quick mode default).
The single commit on the v2 branch is the artifact of record. Update STATE.md's
"Quick Tasks Completed" table with the commit hash post-execution (handled by the
quick-task workflow, not by the executor of this plan).
</output>
